package com.datalake.fieldauth.presentation.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val AppShapes = Shapes(
    small = RoundedCornerShape(8.dp),     // For chips, small badges
    medium = RoundedCornerShape(12.dp),   // For flat cards, dialogs
    large = RoundedCornerShape(28.dp)     // For primary buttons
)
